spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_boot_workshop
    driver-class-name: org.postgresql.Driver
    password:
    username:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

